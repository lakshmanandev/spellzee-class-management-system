import { Navigate, Route, Routes } from 'react-router-dom'; import { useAuth } from './context/AuthContext.jsx'; import Loader from './components/Loader.jsx'; import Layout from './components/Layout.jsx'; import Login from './pages/Login.jsx'; import AdminDashboard from './pages/AdminDashboard.jsx'; import Students from './pages/Students.jsx'; import StudentForm from './pages/StudentForm.jsx'; import Classes from './pages/Classes.jsx'; import ClassForm from './pages/ClassForm.jsx'; import StudentDashboard from './pages/StudentDashboard.jsx';
function Protected({ role, children }) { const { user, loading } = useAuth(); if (loading) return <Loader/>; if (!user) return <Navigate to="/login" replace/>; if (user.role !== role) return <Navigate to={user.role === 'ADMIN' ? '/admin' : '/student'} replace/>; return <Layout>{children}</Layout>; }
export default function App(){ return <Routes><Route path="/login" element={<Login/>}/><Route path="/admin" element={<Protected role="ADMIN"><AdminDashboard/></Protected>}/><Route path="/admin/students" element={<Protected role="ADMIN"><Students/></Protected>}/><Route path="/admin/students/:id" element={<Protected role="ADMIN"><StudentForm/></Protected>}/><Route path="/admin/classes" element={<Protected role="ADMIN"><Classes/></Protected>}/><Route path="/admin/classes/:id" element={<Protected role="ADMIN"><ClassForm/></Protected>}/><Route path="/student" element={<Protected role="STUDENT"><StudentDashboard/></Protected>}/><Route path="*" element={<Navigate to="/login" replace/>}/></Routes>; }
