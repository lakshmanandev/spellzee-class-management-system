import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import toast from 'react-hot-toast'; import api from '../services/api.js'; import Loader from '../components/Loader.jsx'; import StatusBadge from '../components/StatusBadge.jsx';
export default function Students(){const [students,setStudents]=useState();const [q,setQ]=useState(''); const load=()=>api.get('/students',{params:{q}}).then(r=>setStudents(r.data.data.students));useEffect(()=>{const id=setTimeout(load,250);return()=>clearTimeout(id)},[q]);const remove=async id=>{if(!confirm('Deactivate this student? They will no longer be able to log in.'))return;await api.delete(`/students/${id}`);toast.success('Student deactivated');load()};if(!students)return <Loader/>;return <div className="page"><div className="page-head"><div><h2>Students</h2><p>Manage student accounts and course details.</p></div><Link className="primary" to="/admin/students/new">Add student</Link></div><div className="panel"><input className="search" placeholder="Search name, email or course…" value={q} onChange={e=>setQ(e.target.value)}/><div className="table-wrap"><table><thead><tr><th>Student</th><th>Course</th><th>Phone</th><th>Status</th><th></th></tr></thead><tbody>{students.map(s=><tr key={s._id}><td><strong>{s.name}</strong><small>{s.email}</small></td><td>{s.course}</td><td>{s.phone||'—'}</td><td><StatusBadge value={s.status}/></td><td className="actions"><Link to={`/admin/students/${s._id}`}>Edit</Link>{s.status==='ACTIVE'&&<button className="danger-link" onClick={()=>remove(s._id)}>Deactivate</button>}</td></tr>)}</tbody></table></div>{!students.length&&<div className="empty">No students found.</div>}</div></div>}
