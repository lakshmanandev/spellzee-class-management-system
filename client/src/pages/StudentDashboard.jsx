import { useEffect,useState } from 'react'; import toast from 'react-hot-toast'; import { io } from 'socket.io-client'; import api from '../services/api.js'; import Loader from '../components/Loader.jsx'; import ClassTable from '../components/ClassTable.jsx';
export default function StudentDashboard(){const [data,setData]=useState();const load=()=>api.get('/dashboard/student').then(r=>setData(r.data.data));useEffect(()=>{load();const socket=io(import.meta.env.VITE_SOCKET_URL||'http://localhost:5000',{auth:{token:localStorage.getItem('spellzee_token')}});socket.on('class:scheduled',event=>{toast.success(event.message);setData(prev=>prev?{...prev,stats:{...prev.stats,upcoming:prev.stats.upcoming+1},classes:[event.class,...prev.classes]}:prev)});return()=>socket.close()},[]);if(!data)return <Loader/>;const upcoming=data.classes.filter(x=>x.status==='SCHEDULED');const complete=data.classes.filter(x=>x.status==='COMPLETED');return <div className="page"><section className="stats student-stats"><div className="stat"><small>Upcoming classes</small><strong>{data.stats.upcoming}</strong></div><div className="stat"><small>Completed classes</small><strong>{data.stats.completed}</strong></div><div className="stat"><small>Classes attended</small><strong>{data.stats.present}</strong></div></section><section className="panel"><div className="section-head"><div><h2>Upcoming classes</h2><p>Your next learning sessions.</p></div></div><ClassTable classes={upcoming}/></section><section className="panel"><div className="section-head"><div><h2>Completed classes</h2><p>Your recent attendance.</p></div></div><ClassTable classes={complete}/></section></div>}
