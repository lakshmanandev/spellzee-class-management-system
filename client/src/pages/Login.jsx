import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import toast from 'react-hot-toast'; import { useAuth } from '../context/AuthContext.jsx';
export default function Login(){ const { login }=useAuth(); const nav=useNavigate(); const [form,setForm]=useState({email:'admin@spellzee.com',password:'Admin@123'}); const [busy,setBusy]=useState(false); const submit=async e=>{e.preventDefault();setBusy(true);try{const user=await login(form);toast.success('Signed in successfully');nav(user.role==='ADMIN'?'/admin':'/student');}catch(e){toast.error(e.response?.data?.message||'Unable to sign in');}finally{setBusy(false)}}; return <div className="login-page"><section className="login-intro"><div className="brand">spell<span>zee</span></div><div><h1>Run every class with clarity.</h1><p>A focused workspace for scheduling, attendance and students—all in one place.</p></div><div className="intro-note">Built for teams that care about every learner.</div></section><section className="login-panel"><form onSubmit={submit}><h2>Welcome back</h2><p>Sign in to your class workspace.</p><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label><button className="primary full" disabled={busy}>{busy?'Signing in…':'Sign in'}</button><div className="demo"><strong>Demo accounts</strong><span>Admin: admin@spellzee.com / Admin@123</span><span>Student: aarav@spellzee.com / Student@123</span></div></form></section></div>; }
