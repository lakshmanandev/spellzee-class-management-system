import { useEffect,useState } from 'react'; import { useNavigate,useParams } from 'react-router-dom'; import toast from 'react-hot-toast'; import api from '../services/api.js'; import Loader from '../components/Loader.jsx'; const blank={name:'',email:'',phone:'',password:'',course:'',status:'ACTIVE'};
export default function StudentForm(){const {id}=useParams();const nav=useNavigate();const [form,setForm]=useState(blank);const [loading,setLoading]=useState(id!=='new');useEffect(()=>{if(id!=='new')api.get(`/students/${id}`).then(r=>setForm({...r.data.data.student,password:''})).finally(()=>setLoading(false))},[id]);const submit=async e=>{e.preventDefault();try{if(id==='new')await api.post('/students',form);else await api.put(`/students/${id}`,form);toast.success('Student saved');nav('/admin/students')}catch(e){toast.error(e.response?.data?.message||'Could not save student')}};if(loading)return <Loader/>;return <div className="page form-page"><div className="page-head"><div><h2>{id==='new'?'Add student':'Edit student'}</h2><p>Keep account and course details current.</p></div></div><form className="panel form-grid" onSubmit={submit}>{[['name','Name'],['email','Email'],['phone','Phone'],['course','Course'],['password',id==='new'?'Password':'New password (optional)']].map(([key,label])=><label key={key}>{label}<input type={key==='password'?'password':key==='email'?'email':'text'} value={form[key]||''} onChange={e=>setForm({...form,[key]:e.target.value})} required={id==='new'||key!=='password'}/></label>)}<label>Status<select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option>ACTIVE</option><option>INACTIVE</option></select></label><div className="form-actions"><button className="secondary" type="button" onClick={()=>nav('/admin/students')}>Cancel</button><button className="primary">Save student</button></div></form></div>}
